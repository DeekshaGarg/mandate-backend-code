package com.example.restfulservices;

import org.springframework.context.annotation.Configuration;

//import springfox.documentation.swagger2.annotations.EnableSwagger2;

@Configuration
//@EnableSwagger2
public class SwaggerConfig {

	/*
	 * private static final Contact DEFAULT_CONTACT = new Contact("", "", "");
	 * public static final ApiInfo DEFAULT_API_INFO = new
	 * ApiInfo("Api Documentation", "Api Documentation", "1.0", "urn:tos",
	 * DEFAULT_CONTACT, "Apache 2.0", "http://www.apache.org/licenses/LICENSE-2.0",
	 * new ArrayList<VendorExtension>());
	 * 
	 * private static final Set<String> DEFAULT_PRODUCES_AND_CONSUMES = new
	 * HashSet<String>(Arrays.asList("application/xml","application/json"));
	 * 
	 * @Bean public Docket api() { return new Docket(DocumentationType.SWAGGER_2)
	 * .apiInfo(DEFAULT_API_INFO) .produces(DEFAULT_PRODUCES_AND_CONSUMES)
	 * .consumes(DEFAULT_PRODUCES_AND_CONSUMES);
	 
	}*/

	/*
	 * @Primary
	 * 
	 * @Bean public LinkDiscoverers discoverers() { List<LinkDiscoverer> plugins =
	 * new ArrayList<>(); plugins.add(new CollectionJsonLinkDiscoverer()); return
	 * new LinkDiscoverers(SimplePluginRegistry.create(plugins)); }
	 */

}
